<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trabajofinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrabajoFinal">
        <activity android:name=".AlarmaActivity"></activity>
        <activity android:name=".Calcular5Activity" />
        <activity android:name=".Calcular4Activity" />
        <activity android:name=".Calcular3Activity" />
        <activity android:name=".Menu1Activity" />
        <activity android:name=".EvaluacionesActivity" />
        <activity android:name=".ListaCurActivity" />
        <activity android:name=".RegCurActivity" />
        <activity android:name=".AlumnoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CargaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>